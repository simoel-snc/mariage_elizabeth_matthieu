# Google Apps Script — contains the secret invite code
google-apps-script.js

# Setup instructions (not needed in deployment)
SETUP.md

# Claude Code config
CLAUDE.md

# Generated QR codes for the wedding invitation (kept local, not deployed)
invite-qr*.png

# The wedding PIN (read by scripts/generate-qr.py). NEVER commit.
.pin

invite

# Local-only preview thumbnails generated during planning
img/_previews/
